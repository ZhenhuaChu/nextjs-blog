/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */   
    output: 'export',
    reactStrictMode: true,

    basePath: '',
  }
  
  module.exports = nextConfig
