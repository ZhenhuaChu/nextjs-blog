/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    output: 'export',
    reactStricMode: true,

    basePath: 'github-pages',
  }
  
  module.exports = nextConfig
