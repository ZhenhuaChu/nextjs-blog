/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    output: 'export',
    reactStricMode: true,

    basePath: 'zhenhuachu.github.io',
  }
  
  module.exports = nextConfig
